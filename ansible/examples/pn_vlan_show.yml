---
- name: PN-CLI Test
  hosts: testswitches
  user: root
  

  tasks:
  - name: Test VLAN Show CLI commands 
    pn_vlanshow: pn_command='vlan-show' pn_quiet=True 
    register: cmd_output
  - debug: var=cmd_output 
    
