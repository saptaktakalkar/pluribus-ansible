---
- name: PN-CLI trunk Delete
  hosts: newswitches
  user: pluribus
  

  tasks:
  - name: Delete trunk 
    pn_trunk: pn_cliusername=network-admin pn_clipassword=test123 pn_command=trunk-delete pn_name=spine1-to-leaf pn_quiet=True
    register: cmd_output
